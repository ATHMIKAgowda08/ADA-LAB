def coin_row(c):
    n = len(c)
    
    if n == 0:
        return 0
    
    f = [0] * (n + 1)
    f[0] = 0
    f[1] = c[0]
    
    for i in range(2, n + 1):
        f[i] = max(c[i-1] + f[i-2], f[i-1])
    
    return f[n]


coins = [5, 1, 2, 10, 6]
result = coin_row(coins)
print(result)
